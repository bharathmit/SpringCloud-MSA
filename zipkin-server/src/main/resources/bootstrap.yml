spring:
  application:
    name: zipkin-server
  cloud:
    config:
      uri: http://localhost:8080
server:
  port: 9411
      
endpoints.cors.allowed-origins: http://localhost:8040
endpoints.cors.allowed-methods: GET,POST  
      