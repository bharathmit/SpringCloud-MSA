spring:
  application:
    name: product
  cloud:
    config:
      uri: http://localhost:8099
  zipkin:
    base-url: http://localhost:9411/
  sleuth:
    sampler.probability: 1
      
server:
  port: 8010 
    
management:
  endpoints:
    web:
      exposure:
        include: "*"

    
    
  
      
  
